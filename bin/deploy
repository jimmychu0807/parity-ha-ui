#!/usr/bin/env bash
# set -x

USER="jimmychu"
REMOTE="staging.hkwtf.com"
DEPLOY_PATH="/var/www/parity-ha/ui/public/"

echo "Deploying parity-ha-ui to staging..."
ssh ${USER}@${REMOTE} "bash -lc \"rm -rf ${DEPLOY_PATH}*\""
rsync -av build/ ${USER}@${REMOTE}:$DEPLOY_PATH
